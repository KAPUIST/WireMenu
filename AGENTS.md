# WireMenu 작업 안내

- 작업 시작 시 [README](README.md)로 빌드·실행 절차를 확인한다. 코드 수정·리뷰·버그 진단 시 [구조와 검증 기준](docs/ARCHITECTURE.md)을 읽는다.
- 설치본 교체·이동·권한 진단 시 로컬 `OPERATIONS.md`가 있으면 먼저 읽는다. 이 파일은 개인 경로를 포함하며 Git에 올리지 않는다. 파일이 없으면 설치 경로를 사용자와 확인한다.
- 소스는 `Sources`, 아이콘과 번들 설정은 `App`, 검증은 `Tests`가 원본이다. `dist`와 설치된 `.app` 내부를 직접 수정하지 않는다.
- 변경 후 `swift test`와 `./build-app.sh`를 실행한다. UI 변경은 영향받은 동작을 실제 화면에서 확인하고 통과·실패·미검증을 구분한다.
- 네트워크 연결·Wi-Fi 전원·위치 권한·로그인 항목은 사용자 상태다. 테스트 편의를 위해 임의 변경하지 않는다. SSID·IP·비밀번호·개인 화면 캡처를 커밋하지 않는다.
- 문서는 역할별로 유지한다: 사용 안내는 README, 구조·공통 회귀 기준은 ARCHITECTURE, 머신별 기록은 로컬 OPERATIONS. 코드 구조나 검증 방법을 변경하면 해당 문서만 함께 갱신한다.
